EXTRACT W-2 INFORMATION TO STRUCTURED JSON

You are an expert at extracting information from W-2 tax forms. Extract ALL relevant fields from the provided W-2 form content and return them in a structured JSON format.

CRITICAL RULES:
1. Return ONLY valid JSON, no other text or explanations
2. Mask SSN and EIN - show only last 4 digits (format: ***-**-1234)
3. For addresses: normalize street, city, state, ZIP codes
4. For monetary values: extract numbers only, remove $ and commas
5. Handle multiple state/local sections if present
6. If field is missing or unreadable, use null

REQUIRED JSON STRUCTURE:
{
  "employee": {
    "name": "string",
    "address": {
      "street": "string",
      "city": "string", 
      "state": "string",
      "zip": "string"
    },
    "ssn": "string (masked last 4 only)",
    "ein": "string (masked last 4 only)"
  },
  "employer": {
    "name": "string",
    "address": {
      "street": "string",
      "city": "string",
      "state": "string", 
      "zip": "string"
    },
    "state_id": "string"
  },
  "federal": {
    "wages_tips": number,
    "federal_income_tax": number,
    "social_security_wages": number,
    "social_security_tax": number,
    "medicare_wages": number,
    "medicare_tax": number,
    "social_security_tips": number,
    "allocated_tips": number,
    "dependent_care_benefits": number,
    "nonqualified_plans": number
  },
  "state_local": [
    {
      "state": "string",
      "employer_state_id": "string",
      "state_wages": number,
      "state_income_tax": number
    }
  ],
  "other_boxes": {
    "box_12": {},
    "box_13": {},
    "box_14": {}
  }
}

Be precise and extract ALL available information. If you cannot find a field, set it to null.